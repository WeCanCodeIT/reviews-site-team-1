const tagsService = require("../service/tags-service");
const Tags = require("../utils/tags");

class TagsController{

    static getTags(req, res){
        res.render (this.getTags)
        
 
    }
}